# Ruby on Rails - Datenbanken

Installation des Projekts:

```shell
# Optional:
gem install rails
gem install sqlite3

# Notwendig:
bundle install --gemfile Gemfile
rake db:create
rails db:migrate
rails db:seed
rails server
```
